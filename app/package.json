{
  "name": "app",
  "version": "1.0.0",
  "description": "Node express app",
  "main": "src/app.js",
  "scripts": {
    "start": "NODE_ENV=production node dist/app.js",
    "build": "rm -rf dist || true && tsc",
    "dev": "NODE_ENV=development tsx watch src/app.ts",
    "test": "jest --detectOpenHandles --verbose --silent",
    "test-console": "jest --detectOpenHandles --verbose --silent=false"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/<username>/<repo-name>.git"
  },
  "keywords": [],
  "author": "",
  "bugs": {
    "url": "https://github.com/<username>/<repo-name>/issues"
  },
  "homepage": "https://github.com/<username>/<repo-name>#readme",
  "dependencies": {
    "chalk": "^4.1.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "tsx": "^4.11.2",
    "typescript": "^5.4.5",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21"
  }
}
